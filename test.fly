fairytale Fact(n);

once upon the time () {

    n turn into 10;
    count turn into 1000000;
    cur turn into 0;
    tmp turn into 0;
    fell into a dead sleep until (cur weaker count) {
        tmp turn into Fact(n);
        cur turn into cur + 1;
    }

    say the magic number (tmp);

    end();
}

fairytale Fact(n) {

    stone (n not stronger 1)
    go right {
        end(1);
    }

    end(Fact(n - 1) * n);
}