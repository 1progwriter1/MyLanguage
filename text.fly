// fact

fairytale test(a, b);

once upon the time () {

    a turn into 0;
    b turn into 10;

    guess the riddle(a);
    guess the riddle(b);

    say the magic word("Here is the answer: ");
    say the magic number(test(a, b));
    say the magic word("\n");

    end();
}

fairytale test(a, b) {
    end(a * b);
}