// fact

fairytale difference(a, b);

once upon the time () {

    a turn into 0;
    b turn into 10;

    say the magic number (difference(b, a));

    end();
}

fairytale difference(a, b) {
    end(a - b);
}